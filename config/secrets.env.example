# Labki Configuration Secrets
# ============================
# Copy this file to 'secrets.env' and customize it.
# IMPORTANT: secrets.env is gitignored and should NEVER be committed!

# ------------------------------
# Site Identity (First Run Only)
# ------------------------------
# These settings are used only when installing the wiki for the first time.
# Changing them later requires editing LocalSettings.user.php or database values.
MW_SITE_NAME=Labki
MW_SITE_LANG=en

# The full URL of your wiki (use HTTPS in production!)
MW_SERVER=http://localhost:8080

# ------------------------------
# Admin Account (First Run Only)
# ------------------------------
MW_ADMIN_USER=admin
# SECURITY: Generate a strong password! Example: openssl rand -base64 24
MW_ADMIN_PASS=CHANGE_ME_IMMEDIATELY

# ------------------------------
# Database Configuration
# ------------------------------
# SECURITY: Change these defaults before first run!

# MediaWiki database connection (used by the wiki container)
MW_DB_HOST=db
MW_DB_NAME=labki
MW_DB_USER=labki
# SECURITY: Generate a strong password! Example: openssl rand -base64 24
MW_DB_PASSWORD=CHANGE_ME_IMMEDIATELY

# MariaDB container initialization (used by the db container)
# These MUST match the MW_DB_* values above!
MARIADB_DATABASE=labki
MARIADB_USER=labki
MARIADB_PASSWORD=CHANGE_ME_IMMEDIATELY
# SECURITY: Generate a strong root password
MARIADB_ROOT_PASSWORD=CHANGE_ME_IMMEDIATELY

# ------------------------------
# Development Options
# ------------------------------
# Set to 1 to disable the platform's bundled extensions (SMW, etc.)
# This is useful for testing in a "clean" environment.
MW_DISABLE_PLATFORM_EXTENSIONS=0
