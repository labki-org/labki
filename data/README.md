# Data Directory

Store instance-specific data, scripts, or manual backups here.
This directory is persisted in the repository structure but its contents (except this README) should be gitignored if they are large or sensitive.
